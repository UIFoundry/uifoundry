---
title: "CTA 1"
description: "Clean centered call-to-action with header, subheader, and flexible action buttons"
category: "blocks"
---

## Preview

<CTA_1 />

## Props

The CTA_1 block accepts the following configuration from PayloadCMS:

<TypeTable
  type={{
    header: {
      description: "Main call-to-action headline displayed prominently",
      type: "string",
      required: false,
      default: "Ready to Ship Your Next Project?",
    },
    subheader: {
      description: "Supporting description text displayed below the header",
      type: "string",
      required: false,
      default: "Join the developers and founders building faster with UIFoundry. Choose your path and start building today.",
    },
    actions: {
      description: "Array of call-to-action buttons (maximum 2)",
      type: "array",
      default: "2 default actions (Get Developer Template, Start Hosted Trial)",
    },
    "actions[].label": {
      description: "Button text displayed on the call-to-action",
      type: "string",
      required: true,
      default: "Get Started",
    },
    "actions[].href": {
      description: "URL link for the call-to-action button",
      type: "string",
      required: true,
      default: "/",
    },
  }}
/>

## Installation

```bash
npx shadcn@latest add @uifoundry/cta-1
```

If you encounter issues, check the [registry setup guide](/docs/registry-setup) for configuration help.

## Registry Dependencies

This component uses the following registry components:

- [Button](/docs/ui/button) - Customizable button component with variants
- [Header Field](/docs/fields/header-field) - PayloadCMS text field for headers
- [Subheader Field](/docs/fields/subheader-field) - PayloadCMS text field for subheaders
- [Call To Action Pair Field](/docs/fields/call-to-action-pair-field) - PayloadCMS array field for pairs of call-to-action buttons
- [Block Constants](/docs/lib/block-constants) - Block slug and group constants for PayloadCMS blocks

## Dependencies

This component requires the following NPM packages:

- [React](https://github.com/facebook/react) - JavaScript library for building user interfaces
- [Next.js](https://github.com/vercel/next.js) - React framework for production
- [PayloadCMS](https://github.com/payloadcms/payload) - Headless CMS and application framework

## Design Notes

This CTA block features:

- **Clean, centered layout**: All content is center-aligned for maximum impact
- **Flexible spacing**: Responsive padding that scales from mobile to desktop
- **Action button flexibility**: Supports 0-2 action buttons with automatic gap spacing
- **Button variants**: Alternating button styles (outline variant for secondary actions)
- **Balanced typography**: Large, bold header with supporting subheader text
- **Maximum width container**: Content constrained to 5xl for optimal readability

Use this component for simple, effective call-to-action sections that need clear messaging and prominent action buttons. Perfect for landing pages, product pages, or conversion-focused sections.
