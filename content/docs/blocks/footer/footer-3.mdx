---
title: "Footer 3"
description: "Footer with brand logo, social icons, navigation links grouped in columns, and copyright section"
category: "blocks"
---

## Preview

<Footer_3 />

## Props

The Footer_3 block accepts the following props from PayloadCMS:

<TypeTable
  type={{
    brandLogo: {
      type: "MediaField",
      required: false,
      description: "Brand logo with light and dark theme support (see MediaField structure below)",
    },
    copyright: {
      type: "string",
      required: false,
      description: "Copyright text displayed at the bottom of the footer",
      default: "UIFoundry, All rights reserved",
    },
    links: {
      type: "Array<LinkGroup>",
      required: false,
      description: "An array of link groups, each with a title and items (see LinkGroup structure below)",
    },
    socialLinks: {
      type: "Array<SocialLink>",
      required: true,
      description: "Array of social media links with icons (see SocialLink structure below)",
    },
  }}
/>

### MediaField

The brand logo field supports separate images for light and dark themes:

<TypeTable
  type={{
    light: {
      type: "Media",
      required: false,
      description: "Logo image for light theme",
    },
    dark: {
      type: "Media",
      required: false,
      description: "Logo image for dark theme",
    },
  }}
/>

### LinkGroup

Each link group in the `links` array has the following structure:

<TypeTable
  type={{
    group: {
      type: "string",
      description: "Title/heading for the link group (e.g., 'Product', 'Company', 'Legal')",
      required: false,
    },
    items: {
      type: "Array<LinkItem>",
      description: "Array of navigation links within this group",
      required: false,
    },
  }}
/>

### LinkItem

Each navigation link within a group's `items` array has the following structure:

<TypeTable
  type={{
    label: {
      type: "string",
      description: "Display text for the navigation link",
      required: true,
    },
    href: {
      type: "string",
      description: "Target URL for the navigation link",
      required: true,
    },
  }}
/>

### SocialLink

Each social link in the `socialLinks` array has the following structure:

<TypeTable
  type={{
    href: {
      type: "string",
      description: "URL for the social media profile",
      required: true,
    },
    icon: {
      type: "SocialIconKey",
      description: "The social media platform icon to display (Facebook, Twitter, LinkedIn, Instagram, etc.)",
      required: true,
    },
  }}
/>

## Installation

```bash
npx shadcn@latest add @uifoundry/footer-3
```

If you encounter issues, check the [registry setup guide](/docs/guides/registry-setup) for configuration help.

## Registry Dependencies

This component uses the following registry components:

- [Style Utils](/docs/lib/style-utils) - Tailwind utility functions including `cn()` class merger
- [Media Field](/docs/fields/media-field) - PayloadCMS media field with theme support
- [Social Links Field](/docs/fields/social-links-field) - PayloadCMS field for managing social media links with icon selection
- [Block Constants](/docs/lib/block-constants) - Block slug and group constants for PayloadCMS blocks

## Dependencies

This component requires the following NPM packages:

- [PayloadCMS](https://github.com/payloadcms/payload) - Headless CMS and application framework
- [React](https://github.com/facebook/react) - JavaScript library for building user interfaces
- [Next.js](https://github.com/vercel/next.js) - React framework for production
