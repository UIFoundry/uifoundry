---
title: "Pricing 2"
description: "Pricing table with three-column layout, popular badge, and feature lists"
category: "blocks"
---

## Preview

<Pricing_2 />

## Props

The Pricing_2 block accepts the following configuration from PayloadCMS:

<TypeTable
  type={{
    header: {
      description: "Main heading text displayed at the top of the pricing section",
      type: "string",
      required: false,
      default: "Pricing that Scales with You",
    },
    subheader: {
      description: "Supporting description text displayed below the header",
      type: "string",
      required: false,
      default: "Choose the perfect plan for your needs",
    },
    tiers: {
      description: "Array of pricing tier options (minimum 0, maximum 4). Displays in a responsive grid (stacked on mobile, 3-column on desktop).",
      type: "array",
      default: "3 default tiers: Starter ($29/mo), Professional ($79/mo - marked as Popular), Enterprise ($199/mo)",
    },
    "tiers[].name": {
      description: "Name of the pricing plan displayed as the card title",
      type: "string",
      required: true,
    },
    "tiers[].price": {
      description: "Price amount for the plan (numeric value displayed with dollar sign)",
      type: "number",
      required: true,
      default: "0",
    },
    "tiers[].period": {
      description: "Billing period text displayed after price (e.g., 'mo' for month, 'yr' for year)",
      type: "string",
      default: "mo",
    },
    "tiers[].description": {
      description: "Short description text displayed below the price",
      type: "string",
      required: false,
    },
    "tiers[].isPopular": {
      description: "Whether to display a 'Popular' badge at the top of the tier card. Popular tiers use default button variant, others use outline variant.",
      type: "boolean",
      default: "false",
    },
    "tiers[].buttonText": {
      description: "Text displayed on the call-to-action button",
      type: "string",
      required: true,
      default: "Get Started",
    },
    "tiers[].buttonHref": {
      description: "URL link for the call-to-action button",
      type: "string",
      required: true,
      default: "",
    },
    "tiers[].features": {
      description: "Array of feature items displayed with check mark icons (minimum 1, maximum 15 features per tier)",
      type: "array",
    },
    "tiers[].features[].text": {
      description: "Feature description text displayed with a check mark icon",
      type: "string",
      required: true,
    },
  }}
/>

## Installation

```bash
npx shadcn add --registry https://uifoundry.com/r pricing-2
```

For detailed setup instructions, see the [Registry Setup Guide](/docs/registry-setup).

## Registry Dependencies

This component uses the following registry components:

- [Button](/docs/ui/button) - Customizable button component with variants
- [Header Field](/docs/fields/header-field) - PayloadCMS text field for headers
- [Subheader Field](/docs/fields/subheader-field) - PayloadCMS text field for subheaders
- [Description Field](/docs/fields/description-field) - PayloadCMS text field for descriptions
- [Style Utils](/docs/lib/style-utils) - Tailwind utility functions including cn() class merger
- [Block Constants](/docs/lib/block-constants) - Block slug and group constants for PayloadCMS blocks

## Dependencies

This component requires the following NPM packages:

- [React](https://github.com/facebook/react) - JavaScript library for building user interfaces
- [Next.js](https://github.com/vercel/next.js) - React framework for production
- [PayloadCMS](https://github.com/payloadcms/payload) - Headless CMS and application framework
- [Lucide React](https://github.com/lucide-icons/lucide/tree/main/packages/lucide-react) - Beautiful & consistent icon toolkit (provides Check icon for feature lists)

## Usage Notes

### Default Configuration

The Pricing_2 block comes with three pre-configured pricing tiers:

- **Starter** ($29/mo): Perfect for small teams with 5 features including basic analytics and email support
- **Professional** ($79/mo): Marked as Popular, includes 6 features with advanced analytics and priority support
- **Enterprise** ($199/mo): For large organizations with 8 features including unlimited storage and 24/7 support

### Styling Features

- **Popular Badge**: Tiers marked with `isPopular: true` display a gradient badge at the top and use the default button variant (filled) instead of the outline variant
- **Responsive Layout**: Cards stack vertically on mobile and display in a 3-column grid on desktop (md breakpoint)
- **Card Design**: Each tier uses a rounded card with border, muted background, and shadow for depth
- **Feature Lists**: Features display with a dashed divider above and check mark icons beside each item

### Customization Tips

- Set `minRows: 0` allows you to create pricing sections without any tiers (useful for placeholder pages)
- Maximum of 4 pricing tiers ensures optimal layout on standard screen sizes
- Each tier can have up to 15 features, though 5-8 features per tier is recommended for readability
- Button styling automatically adapts based on the `isPopular` flag (default vs outline variant)
