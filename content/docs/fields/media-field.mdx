---
title: "Media Field"
description: "PayloadCMS dual upload field supporting light and dark theme image variants"
category: "fields"
---

## Preview

```typescript
import mediaField from "~/payload/fields/media/config"
import MediaField from "~/payload/fields/media"

// Usage in PayloadCMS block/collection config
const heroMedia = mediaField({
  name: "heroImage",
  label: "Hero Image"
})

// Usage in React component
<MediaField
  media={block.heroImage}
  width={800}
  height={400}
/>
```

## Props

The mediaField function accepts the following configuration options:

<TypeTable
  type={{
    name: {
      description: "Field name for the media field group",
      type: "string",
      default: "media",
    },
    label: {
      description: "Display label in PayloadCMS admin",
      type: "string",
    },
    admin: {
      description: "Admin-specific configuration options",
      type: "AdminConfig",
    },
    access: {
      description: "Access control configuration",
      type: "AccessConfig",
    },
    hooks: {
      description: "Field-level hooks",
      type: "FieldHooks",
    },
  }}
/>

## Installation

```bash
npx shadcn@latest add @uifoundry/media-field
```

If you encounter issues, check the [registry setup guide](/docs/guides/registry-setup) for configuration help.

## Registry Dependencies

This component uses the following registry components:

- [Field Types](/docs/lib/field-types) - TypeScript definitions for PayloadCMS fields
- [Upload Field](/docs/fields/upload-field) - PayloadCMS upload field configuration

## Dependencies

This component requires the following NPM packages:

- [React](https://github.com/facebook/react) - JavaScript library for building user interfaces
- [Next.js](https://github.com/vercel/next.js) - React framework for production
