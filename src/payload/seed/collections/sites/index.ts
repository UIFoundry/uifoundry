import type { BasePayload } from "payload";
// import type { Page } from "~/payload-types";
// import home from "./data/uifoundry/pages/home";

export async function seedSites(_payload: BasePayload) {
	// console.log("homepage: ", home);
	console.log("seeding sites...");
	// const newPage: Page = {
	// 	title: home.title,
	// 	slug: home.slug,
	// 	blocks: [...home.blocks],
	// };
}
